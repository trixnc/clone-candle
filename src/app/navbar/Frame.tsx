import React from "react";
import Container from "../components/Container";

export default function Frame() {
    return (
        <div className="w-full h-250">
            <div className="flex items-center justify-center font-bold">Trusted by teams at</div>
            <p className="flex items-center justify-center text-gray-500">More than 500,000 people across 60,000 companies are active with Reclaim</p>
        </div>
    );
}